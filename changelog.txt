// $Id$

August 2008
-----------
 * Change case of package 'User interface' (aaron).

May 2008
--------
 * grouping settings (aaron).

February 2008
-------------
 * add css & begin manual refresh ability (aaron).
 * add js function to optionally/manually reload all content (aaron).
 * only load automatic refresh when block is enabled on a page (aaron).
 * automatic refresh js working (aaron).
 * begin js (aaron).
 * add callback for block_refresh/$block/$delta (aaron).
 * add settings form to block admin forms (aaron).
 * begin implementation of block refresh (aaron).
